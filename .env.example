# Copy to .env.local and fill in. .env.local is gitignored — never commit a real key.
ANTHROPIC_API_KEY=your_key_here

# Without a key the app still runs: diagnose() falls back to lib/diagnosis/cache.json.
